jQuery-tree
===========

jQuery-tree is a jQuery plugin to make an HTML unorder list (<ul>) in a tree.

The state of expand path of the tree is save in a cookie. To enable this features include the jQuery crop plugin.

See the example (index.html).